addons:
  postgresql: "13.3"
before_install:
  - "chmod +x mvnw"
dist: trusty
install:
  - "mvn -N io.takari:maven:wrapper"
  - "./mvnw install -DskipTests=true -Dmaven.javadoc.skip=true -B -V"
jdk:
  - oraclejdk11
language: java
services:
  - postgresql
sudo: true
